name: Build and Test
on:
  [push]
defaults:
  run:
    shell: bash

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run test
        run: |
          source test_env && printenv | grep SERVER_ && docker-compose up --build